from app.agent.god import GodAgent


class ArcMichael(GodAgent):
    """Arcangel responsible for Python code generation and testing."""

    name: str = "arc_michael"
    description: str = "Generates and validates Python code"
